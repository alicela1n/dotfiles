return {
  {
    "folke/noice.nvim",
    opts = {
      cmdline = {
        view = "cmdline",  -- puts it back at the bottom like normal vim
      },
      messages = {
        enabled = false,   -- optional: disable message routing through noice
      },
    },
  },
}
